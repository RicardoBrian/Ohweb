# 점검·할 일 체크리스트 (우선순위 순)

> 2026-09-23 보안·버그 수정 이후 기준. **위에서부터 순서대로** 진행한다.
> 순서를 바꾸면 로그인이 막힐 수 있는 단계가 있다(특히 1-5 → 1-8).
>
> **되돌리기:** 규칙을 붙여넣은 뒤 뭔가 깨지면 Firebase 콘솔 → Firestore → 규칙 화면
> 왼쪽의 **규칙 기록(버전 목록)**에서 직전 버전을 골라 게시하면 즉시 원래대로 돌아간다.

---

## 🔴 1순위 — 오늘 (시험 전에 반드시)

### 1-1. 배포가 됐는지 확인
- [ ] Cloudflare 대시보드 → Workers & Pages → **ohinfo** → Deployments: 가장 위 배포가 `Success`이고 커밋 메시지가 최근 것(`규칙 파일 머리말 갱신` 이후)인지
- [ ] 같은 방법으로 **ohweb** 프로젝트도 확인
- 실패로 떠 있으면 → `Retry deployment`

### 1-2. 채점 서버 키 확인
- [ ] Cloudflare → **ohinfo** → Settings → Variables and Secrets → **Production**에 `FIREBASE_SERVICE_ACCOUNT_KEY`가 있는지
- 없거나 방금 새로 넣었으면 → 넣은 뒤 **재배포**해야 적용된다(1-1의 Retry)
- 이게 없으면 시험 제출이 전부 실패한다

### 1-3. 가짜 시험으로 제출까지 끝까지 해보기 ⭐ 가장 중요
테스트용 학생 계정 하나로:
- [ ] admin에서 시험 만들기: 객관식 1개 + 서술형 1개 (+ 코드형 1개가 있으면 같이)
- [ ] 그 학생 반으로 배정, 상태 ON
- [ ] 학생으로 kakainfo.com 로그인 → 시험 목록에 보이는지
- [ ] 입장 → 답 입력 → **새로고침** → 답이 그대로 남아 있는지(자동 임시저장)
- [ ] 다른 탭 갔다 오기 → "이탈 감지" 배너가 뜨는지
- [ ] **제출** → "제출이 완료되었습니다" 화면이 뜨는지
- [ ] admin → 결과 탭 → 그 학생 상태 `제출`, 점수가 들어와 있는지
- [ ] 학생으로 같은 시험 다시 제출 시도 → "이미 제출한 시험입니다"로 막히는지

**실패하면:** 경고창의 `[ ... ]` 대괄호 안 문구를 그대로 복사해서 전달. 그 문구가 원인이다.
실패해도 답안은 admin 결과 탭에 `답안 보관됨`으로 남고 **보관 답안 반영** 버튼으로 제출 처리할 수 있다.

### 1-4. (선택) 규칙 테스트 돌리기
- [ ] Java가 설치된 PC에서 `cd ohweb-rules-test && npm install && npm test`
- 전부 `전부 통과`면 OK. 이미 이번 수정 기준으로는 통과를 확인했다

### 1-5. 보안 규칙 1단계 적용
- [ ] 레포의 `ohweb-firestore.rules` 파일 **전체**를 복사
- [ ] Firebase 콘솔 → Firestore Database → **규칙** 탭에 전체 덮어쓰기 → **게시**
- ⚠ 이 단계에서는 `// PHASE2:` 줄을 **건드리지 않는다**

### 1-6. 규칙 적용 직후 기능 점검 (학생 계정으로)
- [ ] 로그인 (학교/학년/반/번호 목록이 뜨는지)
- [ ] 홈 화면, 마이페이지 사진 변경
- [ ] **1:1 질문**: 메시지 보내기 → admin(학생 질문)에서 보이고 답장 → 학생 화면에 도착
- [ ] **시험 목록** 열기, 이미 본 시험 결과 보기
- [ ] **게시판(ohdlet)**: 수업 입장, 글쓰기, 내 글 수정·삭제, 좋아요, 댓글 달기·삭제
- [ ] **게시판 고정**: 마스터(isMaster) 계정으로 고정 링크가 보이고 고정되는지 (이제 비밀번호 9600은 없음)
- [ ] **설문**: formlab에서 설문 만들기 → 링크로 다른 학생이 응답 → 만든 학생이 결과 보기 → 응답 1건 삭제
- [ ] 비로그인 허용 설문이면 로그아웃 상태로도 응답되는지
- [ ] admin: 학생 목록, 시험 결과, 설문 관리, 학생 질문 화면이 전부 뜨는지

하나라도 깨지면 → 위 **되돌리기** 방법으로 직전 규칙 복구 후 어느 항목인지 전달.

### 1-7. 공개용 학생 명단 만들기 + 평문 비밀번호 정리
- [ ] admin.kakainfo.com → **학생** 탭 열기 (열리는 순간 공개용 명단이 자동 생성됨)
- [ ] "평문 비밀번호가 남은 학생 N명" 알림이 뜨면 → **평문 비밀번호 정리** 버튼 → 확인
- [ ] 완료 메시지 확인 → 학생 탭을 다시 열었을 때 그 알림이 **안 뜨는지**
- [ ] Firebase 콘솔 → Firestore → 데이터: `student_directory` 문서 수가 `students` 문서 수와 같은지
- 실패한 학생이 있으면 버튼을 한 번 더 누르면 실패분만 재시도

### 1-8. 보안 규칙 2단계 적용 (학생 이름·사진 비공개)
**1-7이 끝난 뒤에만.** 먼저 하면 옛 계정 학생이 로그인을 못 한다.
- [ ] `ohweb-firestore.rules`에서 아래 한 줄을 찾아서
  ```
  allow read: if true; // PHASE2: allow read: if isAdmin() || isOwnStudentAuth(id);
  ```
  이렇게 바꾼다
  ```
  allow read: if isAdmin() || isOwnStudentAuth(id);
  ```
- [ ] 전체를 다시 Firebase 콘솔 규칙 탭에 붙여넣고 **게시**
- [ ] 1-6 점검 중 **로그인·회원가입·게시판**을 다시 확인
  - 게시판 댓글의 다른 학생 사진이 이니셜로 바뀌는 건 정상(의도한 변경)
- [ ] 레포 파일도 같은 내용으로 바꿔서 커밋해 둔다(콘솔과 레포가 달라지지 않게)

---

## 🟠 2순위 — 이번 주

### 2-1. 코드형 문제 실행 살리기
지금은 코드 실행 서버(Piston 공개 API)가 허가제로 바뀌어 **실행 버튼이 안 되고, 코드형은 수동 채점**으로 넘어간다.
- [ ] 방법 A: Piston 운영자(EngineerMan, Discord)에게 **교육용 토큰** 요청
- [ ] 방법 B: Piston을 직접 설치(서버 필요)
- [ ] 받은 값을 Cloudflare **ohinfo** → Variables에 추가 → 재배포
  - `PISTON_URL` (직접 설치한 경우 주소, 예: `https://내서버/api/v2/piston`)
  - `PISTON_TOKEN` (토큰)
- [ ] 가짜 시험에서 코드형 **실행** 버튼과 제출 채점 확인
- 해결 전까지: 코드형이 들어간 시험은 admin 결과 탭 **채점**에서 직접 점수 입력

### 2-2. 게시판 인덱스 만들기 (읽기 비용 절감)
- [ ] Firebase 콘솔 → Firestore → 색인 → 복합 색인 추가:
  컬렉션 `posts`, 필드 `classTitle` 오름차순 + `updatedAt` 오름차순
- 없어도 동작은 하지만 매번 전체를 다시 읽어서 무료 한도를 빨리 쓴다

### 2-3. 사용량 경보 걸기
- [ ] Firebase 콘솔 → 사용량 및 결제에서 Firestore 읽기량 확인
- [ ] (요금제가 Blaze면) 예산 알림 설정
- 무료 한도(하루 읽기 5만 건)를 넘으면 그날은 사이트 전체가 멈춘다

### 2-4. "고치면 딴 데 터지는" 구조 바꾸기 ⭐ 재발 방지의 핵심
- [ ] 앞으로 작업은 `main`에 바로 올리지 않고 **브랜치 → PR → 확인 → 합치기**
- [ ] Cloudflare 각 프로젝트에서 **Preview 배포** 켜기 → PR마다 미리보기 주소로 먼저 확인
- [ ] GitHub Actions로 PR마다 `ohweb-rules-test` 자동 실행 (요청하면 설정해 줌)
- [ ] (나중에) 규칙도 손으로 붙여넣지 말고 Firebase CLI로 테스트 통과 시에만 배포

---

## 🟡 3순위 — 결정이 필요한 남은 구멍

| 항목 | 지금 상태 | 선택지 |
|---|---|---|
| 정산 앱(ohsettle) | 로그인 없음 → 누구나 정산 내역(이름·금액) 읽기·수정 가능 | 로그인 붙이기 / 계속 쓰는지 확인 후 정리 |
| 파일 업로드 비밀키 | Apps Script 비밀키가 학생 페이지 소스에 노출 | Apps Script에서 키 교체 + 서버 경유로 변경 |
| 미가입 학생 계정 가로채기 | 아직 가입 안 한 학생 번호로 남이 먼저 가입 가능 | Firebase에서 누구나 가입 끄기 + 서버에서 가입 처리 |
| 시험 입장 코드 | 브라우저에서 확인 → 마음먹으면 알아낼 수 있음 | 서버에서 확인하도록 변경(영향: 시험 미리 입장 정도) |
| 로그인 5회 잠금 | 로그인 화면에서만 적용 | Firebase 자체 속도 제한이 있어 급하지 않음 |

---

## 🔁 매 시험 전 (반복)
- [ ] 시험 전날: 테스트 학생 계정으로 **1-3 가짜 시험** 한 번
- [ ] 시험 당일 시작 전: 배정 상태 ON, 대상 학년/반 맞는지
- [ ] 코드형이 있으면: 실행 버튼 한 번 눌러보기 (2-1 해결 전이면 수동 채점 예정으로 안내)
- [ ] 제출 실패 학생이 나오면: 사진 찍게 하지 말고 → admin 결과 탭 **답안 보관됨** → **보관 답안 반영**
